export const getRow = (content) => {
    return (
        `<div class="row">
            ${content}
        </div>`
    );
};

export const getCol = (content) => {
    return (
        `<div class="col-sm">
            ${content}
        </div>`
    );
};
